# RAG_youtube_itmo

добавление новых зависимостей
`poetry add numpy`

установка зависимостей
`make requirements`

форматирование (без него не пустит залить код)
`make format`

запуск бота
`make run-bot`
